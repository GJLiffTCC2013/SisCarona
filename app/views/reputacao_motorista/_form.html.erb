<%= form_for(@reputacao_motoristum) do |f| %>
  <% if @reputacao_motoristum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reputacao_motoristum.errors.count, "error") %> prohibited this reputacao_motoristum from being saved:</h2>

      <ul>
      <% @reputacao_motoristum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :carona_id %><br />
    <%= f.text_field :carona_id %>
  </div>
  <div class="field">
    <%= f.label :motorista_id %><br />
    <%= f.text_field :motorista_id %>
  </div>
  <div class="field">
    <%= f.label :justificativa %><br />
    <%= f.text_area :justificativa %>
  </div>
  <div class="field">
    <%= f.label :ponto %><br />
    <%= f.text_field :ponto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
